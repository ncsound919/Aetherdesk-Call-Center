# ============================================================
# AetherDesk — pip compatibility shim
# ============================================================
# pyproject.toml is the single source of truth for dependencies.
#
# To install for development:
#   pip install -e ".[dev]"
#
# To install with ML extras:
#   pip install -e ".[dev,ml]"
#
# To regenerate a pinned lockfile (recommended for deployments):
#   pip-compile pyproject.toml --extra dev --extra ml -o requirements.lock
#
# This file exists only for tools that require a flat requirements.txt
# and should NOT be edited directly.
-e .
